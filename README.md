Shell 脚本库
这是一个 Shell 脚本库，包含多种实用功能的脚本集合。

使用方式
bash
# 远程执行
bash <(curl -sSfLk https://your-domain.com/script.sh)

# 克隆到本地
git clone https://gitee.com/your-username/shell-scripts.git
cd shell-scripts
bash script.sh
